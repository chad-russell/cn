version: '3.8'

services:
  stumpdle:
    build:
      context: /home/crussell/cn/docker/swarm/stumpdle
      dockerfile: Dockerfile
    image: stumpdle:latest
    container_name: stumpdle
    ports:
      - "3177:80"
    restart: unless-stopped
    deploy:
      placement:
        constraints:
          - node.hostname == k2
