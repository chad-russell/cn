# SearXNG minimal settings with JSON support
# See: https://docs.searxng.org/admin/settings/settings.html

search:
  formats:
    - html
    - json

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "ultrasecretkey"

redis:
  url: redis://searxng-valkey:6379/0
