import { hub, thinkpad } from "./brunch.bri";

export const project = {
  name: "brunch-default",
  version: "0.0.1",
  dependencies: {
    brunch: { path: "../src" },
  },
};

export { thinkpad, hub };

// Default is intentionally empty — always specify a target:
//   brunch apply ./config --target hub
//   brunch apply ./config --target thinkpad
export { default } from "./brunch.bri";
