import { ai, hub, thinkpad } from "./brunch.bri";

export const project = {
  name: "brunch-default",
  version: "0.0.1",
  dependencies: {
    brunch: { path: "../src" },
  },
};

export { ai, thinkpad, hub };

// No default export; the CLI requires an explicit target:
//   brunch apply ./config --target ai
//   brunch apply ./config --target hub
//   brunch apply ./config --target thinkpad
