[Unit]
Description=Daily restic backup timer for think

[Timer]
OnCalendar=daily
# Run even if missed while asleep/offline (fires on next wake/boot).
Persistent=true
RandomizedDelaySec=1h
Unit=restic-backup.service

[Install]
WantedBy=timers.target
