;; init.scm — Steel startup script. Runs after helix.scm is loaded and has
;; access to the running editor via *helix.cx*.
;;
;; Example (uncomment to enable the recent-files plugin):
;;
;;   (require "cogs/recentf.scm")
;;   (recentf-snapshot)
;;
;; See https://github.com/mattwparas/helix-config/init.scm for a real-world
;; example.
