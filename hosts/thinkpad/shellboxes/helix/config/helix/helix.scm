;; helix.scm — Steel command module (stateless).
;;
;; Functions you `provide` here become `:` commands and can also be bound to
;; keys. This module has no direct access to the editor context; an invoked
;; function receives `cx` as its first argument.
;;
;; See https://github.com/mattwparas/helix-config/helix.scm for a real-world
;; example and the full set of built-in `helix.*` functions.
