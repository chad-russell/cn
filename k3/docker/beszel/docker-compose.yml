services:
  beszel:
    image: henrygd/beszel:latest
    container_name: beszel
    restart: always
    ports:
      - "8090:8090"
    volumes:
      - beszel-data:/beszel_data
    environment:
      - TZ=America/New_York

volumes:
  beszel-data:

