# Environment files (decrypted)
env.*
.env
*.env

# Rust
**/target/

# Decrypted secrets
secrets/*.yml
secrets/*.env

# Private keys (never commit — the nebula/.gitignore handles its own,
# but catch any stray keys elsewhere in the repo)
*.key
*.pem
*.p12

# Editor / IDE directories
.vscode/
.idea/
*.swp
*.swo
*~

# OS artifacts
.DS_Store

# Nix
result

# Archive
.archived

# Images / composefs descriptors (build artifacts)
*.cfs

# Python
**/__pycache__

# State dirs
hosts/thinkpad/image/state/
