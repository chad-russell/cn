apiVersion: v1
kind: Secret
metadata:
  name: openclaw-secrets
  namespace: openclaw
  labels:
    app: openclaw
type: Opaque
stringData:
  # IMPORTANT: These secrets are committed to git for simplicity
  # Alternative: Use secrets.yaml (gitignored) and kubectl apply separately
  anthropic-api-key: "sk-ant-api03-placeholder-key-replace-with-real-key"
  openai-api-key: "sk-placeholder-key-replace-with-real-key"
  # IMPORTANT: This secret is committed to git for simplicity
  discord-bot-token: "MTIzNDU2Nzg5MDABCD-placeholder-token-replace-with-real-token"
